import Messages from "../utils/Messages";

export default function MessageList() {
  return (
    <div className="messageList">
      <Messages name="jhon" />
      <Messages name="jessy" />
      <Messages name="matty" />
      <Messages name="rafa" />
    </div>
  );
}
